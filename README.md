# congressional_bills

Using machine learning methods to analyze congressional bills and determine if they passed into law

# files to grade:
Bayesian Modeling done in `bills_project.ipynb`

CNN Modeling done in `bills_cnn_R.rmd` with `bill_sums.csv` (which was created in `bills_cnn.ipynb`)
